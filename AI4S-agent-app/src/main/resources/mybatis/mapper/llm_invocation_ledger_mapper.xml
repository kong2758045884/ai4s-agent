<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wwz.ai.infrastructure.dao.ai4s.ILlmInvocationLedgerDao">

    <resultMap id="LlmInvocationMap" type="org.wwz.ai.domain.agent.ledger.entity.LlmInvocation">
        <id column="id" property="id"/>
        <result column="run_id" property="runId"/>
        <result column="invocation_seq" property="invocationSeq"/>
        <result column="agent_name" property="agentName"/>
        <result column="step_no" property="stepNo"/>
        <result column="call_kind" property="callKind"/>
        <result column="streaming" property="streaming"/>
        <result column="model_name" property="modelName"/>
        <result column="response_text" property="responseText"/>
        <result column="reasoning_content" property="reasoningContent"/>
        <result column="tool_call_count" property="toolCallCount"/>
        <result column="prompt_tokens" property="promptTokens"/>
        <result column="completion_tokens" property="completionTokens"/>
        <result column="total_tokens" property="totalTokens"/>
        <result column="cached_prompt_tokens" property="cachedPromptTokens"/>
        <result column="prompt_text_tokens" property="promptTextTokens"/>
        <result column="prompt_audio_tokens" property="promptAudioTokens"/>
        <result column="prompt_image_tokens" property="promptImageTokens"/>
        <result column="completion_text_tokens" property="completionTextTokens"/>
        <result column="completion_audio_tokens" property="completionAudioTokens"/>
        <result column="reasoning_tokens" property="reasoningTokens"/>
        <result column="system_fingerprint" property="systemFingerprint"/>
        <result column="est_total_tokens" property="estTotalTokens"/>
        <result column="est_system_tokens" property="estSystemTokens"/>
        <result column="est_message_tokens" property="estMessageTokens"/>
        <result column="est_tool_tokens" property="estToolTokens"/>
        <result column="message_count" property="messageCount"/>
        <result column="tool_count" property="toolCount"/>
        <result column="cache_status" property="cacheStatus"/>
        <result column="cache_risk_flags" property="cacheRiskFlags"/>
        <result column="finish_reason" property="finishReason"/>
        <result column="status" property="status"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="started_at" property="startedAt"/>
        <result column="finished_at" property="finishedAt"/>
        <result column="duration_ms" property="durationMs"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <insert id="insertLlmInvocation" parameterType="org.wwz.ai.domain.agent.ledger.entity.LlmInvocation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_agent_llm_invocation (
            run_id, invocation_seq, agent_name, step_no, call_kind, streaming, model_name, response_text, reasoning_content,
            tool_call_count, prompt_tokens, completion_tokens, total_tokens,
            cached_prompt_tokens, prompt_text_tokens, prompt_audio_tokens, prompt_image_tokens,
            completion_text_tokens, completion_audio_tokens, reasoning_tokens,
            system_fingerprint, est_total_tokens, est_system_tokens, est_message_tokens, est_tool_tokens,
            message_count, tool_count, cache_status, cache_risk_flags,
            finish_reason, status, error_msg,
            started_at, finished_at, duration_ms, create_time, update_time, deleted
        ) VALUES (
            #{runId}, #{invocationSeq}, #{agentName}, #{stepNo}, #{callKind}, #{streaming}, #{modelName}, #{responseText}, #{reasoningContent},
            #{toolCallCount}, #{promptTokens}, #{completionTokens}, #{totalTokens},
            #{cachedPromptTokens}, #{promptTextTokens}, #{promptAudioTokens}, #{promptImageTokens},
            #{completionTextTokens}, #{completionAudioTokens}, #{reasoningTokens},
            #{systemFingerprint}, #{estTotalTokens}, #{estSystemTokens}, #{estMessageTokens}, #{estToolTokens},
            #{messageCount}, #{toolCount}, #{cacheStatus}, #{cacheRiskFlags},
            #{finishReason}, #{status}, #{errorMsg},
            #{startedAt}, #{finishedAt}, #{durationMs}, NOW(), NOW(), 0
        )
    </insert>

    <update id="updateLlmInvocationFinish" parameterType="org.wwz.ai.domain.agent.ledger.entity.LlmInvocation">
        UPDATE ai_agent_llm_invocation
        SET status = #{status},
            response_text = #{responseText},
            reasoning_content = #{reasoningContent},
            tool_call_count = #{toolCallCount},
            prompt_tokens = #{promptTokens},
            completion_tokens = #{completionTokens},
            total_tokens = #{totalTokens},
            cached_prompt_tokens = #{cachedPromptTokens},
            prompt_text_tokens = #{promptTextTokens},
            prompt_audio_tokens = #{promptAudioTokens},
            prompt_image_tokens = #{promptImageTokens},
            completion_text_tokens = #{completionTextTokens},
            completion_audio_tokens = #{completionAudioTokens},
            reasoning_tokens = #{reasoningTokens},
            cache_status = #{cacheStatus},
            cache_risk_flags = #{cacheRiskFlags},
            finish_reason = #{finishReason},
            error_msg = #{errorMsg},
            finished_at = #{finishedAt},
            duration_ms = CASE
                WHEN started_at IS NULL OR #{finishedAt} IS NULL THEN duration_ms
                ELSE TIMESTAMPDIFF(MICROSECOND, started_at, #{finishedAt}) / 1000
            END,
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="queryByRunId" resultMap="LlmInvocationMap">
        SELECT *
        FROM ai_agent_llm_invocation
        WHERE run_id = #{runId}
          AND deleted = 0
        ORDER BY invocation_seq ASC, id ASC
    </select>

    <select id="queryByRunIds" resultMap="LlmInvocationMap">
        SELECT *
        FROM ai_agent_llm_invocation
        WHERE deleted = 0
          AND run_id IN
        <foreach collection="runIds" item="runId" open="(" separator="," close=")">
            #{runId}
        </foreach>
        ORDER BY run_id ASC, invocation_seq ASC, id ASC
    </select>

</mapper>
