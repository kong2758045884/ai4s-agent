<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wwz.ai.infrastructure.dao.ai4s.IUserQuestionDao">

    <resultMap id="UserQuestionMap" type="org.wwz.ai.infrastructure.dao.ai4s.po.UserQuestionPO">
        <id column="id" property="id"/>
        <result column="question_id" property="questionId"/>
        <result column="visitor_id" property="visitorId"/>
        <result column="session_id" property="sessionId"/>
        <result column="source_run_id" property="sourceRunId"/>
        <result column="source_request_id" property="sourceRequestId"/>
        <result column="tool_invocation_id" property="toolInvocationId"/>
        <result column="tool_call_id" property="toolCallId"/>
        <result column="questions_json" property="questionsJson"/>
        <result column="answers_json" property="answersJson"/>
        <result column="status" property="status"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="resume_request_id" property="resumeRequestId"/>
        <result column="resume_context_json" property="resumeContextJson"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <insert id="insert" parameterType="org.wwz.ai.infrastructure.dao.ai4s.po.UserQuestionPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_agent_user_question (
            question_id, visitor_id, session_id, source_run_id, source_request_id,
            tool_invocation_id, tool_call_id, questions_json, answers_json, status,
            expires_at, resume_request_id, resume_context_json, create_time, update_time, deleted
        ) VALUES (
            #{questionId}, #{visitorId}, #{sessionId}, #{sourceRunId}, #{sourceRequestId},
            #{toolInvocationId}, #{toolCallId}, #{questionsJson}, #{answersJson}, #{status},
            #{expiresAt}, #{resumeRequestId}, #{resumeContextJson}, NOW(), NOW(), 0
        )
    </insert>

    <select id="selectByQuestionId" resultMap="UserQuestionMap">
        SELECT *
        FROM ai_agent_user_question
        WHERE question_id = #{questionId}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByResumeRequestId" resultMap="UserQuestionMap">
        SELECT *
        FROM ai_agent_user_question
        WHERE resume_request_id = #{resumeRequestId}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectOpenBySessionId" resultMap="UserQuestionMap">
        SELECT *
        FROM ai_agent_user_question
        WHERE session_id = #{sessionId}
          AND deleted = 0
          AND status IN ('PENDING', 'RESUME_PENDING', 'RESUMING', 'ANSWERED')
        ORDER BY create_time DESC, id DESC
    </select>

    <select id="countOpenBySessionId" resultType="int">
        SELECT COUNT(1)
        FROM ai_agent_user_question
        WHERE session_id = #{sessionId}
          AND deleted = 0
          AND status IN ('PENDING', 'RESUME_PENDING', 'RESUMING')
    </select>

    <update id="casAnswerPending">
        UPDATE ai_agent_user_question
        SET status = 'RESUME_PENDING',
            answers_json = #{answersJson},
            resume_request_id = #{resumeRequestId},
            update_time = NOW()
        WHERE question_id = #{questionId}
          AND deleted = 0
          AND status = 'PENDING'
          <if test="visitorId != null and visitorId != ''">
              AND (visitor_id IS NULL OR visitor_id = #{visitorId})
          </if>
    </update>

    <update id="casClaimResume">
        UPDATE ai_agent_user_question
        SET status = 'RESUMING',
            update_time = NOW()
        WHERE resume_request_id = #{resumeRequestId}
          AND deleted = 0
          AND status = 'RESUME_PENDING'
          <if test="visitorId != null and visitorId != ''">
              AND (visitor_id IS NULL OR visitor_id = #{visitorId})
          </if>
    </update>

    <update id="markAnswered">
        UPDATE ai_agent_user_question
        SET status = 'ANSWERED',
            update_time = NOW()
        WHERE question_id = #{questionId}
          AND deleted = 0
          AND status = 'RESUMING'
    </update>

    <update id="markFailed">
        UPDATE ai_agent_user_question
        SET status = #{status},
            update_time = NOW()
        WHERE question_id = #{questionId}
          AND deleted = 0
    </update>

    <update id="casCancel">
        UPDATE ai_agent_user_question
        SET status = 'CANCELLED',
            update_time = NOW()
        WHERE question_id = #{questionId}
          AND deleted = 0
          AND status IN
          <foreach collection="fromStatuses" item="st" open="(" separator="," close=")">
              #{st}
          </foreach>
          <if test="visitorId != null and visitorId != ''">
              AND (visitor_id IS NULL OR visitor_id = #{visitorId})
          </if>
    </update>
</mapper>
