<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.wwz.ai.infrastructure.dao.ai4s.IArtifactLedgerDao">

    <resultMap id="ArtifactRecordMap" type="org.wwz.ai.domain.agent.ledger.entity.ArtifactRecord">
        <id column="id" property="id"/>
        <result column="run_id" property="runId"/>
        <result column="request_id" property="requestId"/>
        <result column="tool_invocation_id" property="toolInvocationId"/>
        <result column="tool_call_id" property="toolCallId"/>
        <result column="artifact_role" property="artifactRole"/>
        <result column="visibility" property="visibility"/>
        <result column="source_type" property="sourceType"/>
        <result column="source_name" property="sourceName"/>
        <result column="file_name" property="fileName"/>
        <result column="storage_key" property="storageKey"/>
        <result column="download_url" property="downloadUrl"/>
        <result column="preview_url" property="previewUrl"/>
        <result column="mime_type" property="mimeType"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_hash" property="fileHash"/>
        <result column="metadata_json" property="metadataJson"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <insert id="batchInsertArtifacts">
        INSERT IGNORE INTO ai_agent_artifact (
            run_id, request_id, tool_invocation_id, tool_call_id, artifact_role, visibility, source_type, source_name,
            file_name, storage_key, download_url, preview_url, mime_type, file_size, file_hash, metadata_json,
            create_time, update_time, deleted
        ) VALUES
        <foreach collection="records" item="record" separator=",">
            (
                #{record.runId}, #{record.requestId}, #{record.toolInvocationId}, #{record.toolCallId}, #{record.artifactRole}, #{record.visibility},
                #{record.sourceType}, #{record.sourceName}, #{record.fileName}, #{record.storageKey}, #{record.downloadUrl},
                #{record.previewUrl}, #{record.mimeType}, #{record.fileSize}, #{record.fileHash}, #{record.metadataJson},
                NOW(), NOW(), 0
            )
        </foreach>
    </insert>

    <select id="queryByRunId" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE run_id = #{runId}
          AND deleted = 0
        ORDER BY create_time ASC, id ASC
    </select>

    <select id="queryByRunIds" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND run_id IN
        <foreach collection="runIds" item="runId" open="(" separator="," close=")">
            #{runId}
        </foreach>
        ORDER BY run_id DESC, create_time ASC, id ASC
    </select>

    <select id="queryByToolInvocationIds" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND artifact_role = 'output'
          AND visibility = 'visible'
          AND tool_invocation_id IN
        <foreach collection="toolInvocationIds" item="toolInvocationId" open="(" separator="," close=")">
            #{toolInvocationId}
        </foreach>
        ORDER BY tool_invocation_id ASC, create_time ASC, id ASC
    </select>

    <select id="queryInputArtifactsByRunIds" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND artifact_role = 'input'
          AND visibility = 'visible'
          AND run_id IN
        <foreach collection="runIds" item="runId" open="(" separator="," close=")">
            #{runId}
        </foreach>
        ORDER BY run_id ASC, create_time ASC, id ASC
    </select>

    <select id="queryOutputArtifactsByToolInvocationId" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND artifact_role = 'output'
          AND visibility = 'visible'
          AND tool_invocation_id = #{toolInvocationId}
        ORDER BY create_time ASC, id ASC
    </select>

    <select id="queryOutputArtifactsByRunIdAndToolCallId" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND artifact_role = 'output'
          AND visibility = 'visible'
          AND run_id = #{runId}
          AND tool_call_id = #{toolCallId}
        ORDER BY create_time ASC, id ASC
    </select>

    <select id="queryOutputArtifactsByRequestIdAndToolCallId" resultMap="ArtifactRecordMap">
        SELECT *
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND artifact_role = 'output'
          AND visibility = 'visible'
          AND request_id = #{requestId}
          AND tool_call_id = #{toolCallId}
        ORDER BY create_time ASC, id ASC
    </select>

    <select id="countBySourceName" resultType="int">
        SELECT COUNT(DISTINCT request_id, tool_call_id)
        FROM ai_agent_artifact
        WHERE deleted = 0
          AND artifact_role = 'output'
          AND visibility = 'visible'
          AND source_name = #{sourceName}
    </select>

    <select id="queryBatchHeadsBySourceName" resultMap="ArtifactRecordMap">
        SELECT a.*
        FROM ai_agent_artifact a
        INNER JOIN (
            SELECT MIN(id) AS head_id
            FROM ai_agent_artifact
            WHERE deleted = 0
              AND artifact_role = 'output'
              AND visibility = 'visible'
              AND source_name = #{sourceName}
            GROUP BY request_id, tool_call_id
            ORDER BY MIN(create_time) DESC, MIN(id) DESC
            LIMIT #{offset}, #{limit}
        ) heads ON heads.head_id = a.id
        ORDER BY a.create_time DESC, a.id DESC
    </select>

</mapper>
