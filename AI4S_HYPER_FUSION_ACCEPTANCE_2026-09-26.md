# 战略图谱融合与界面修复验收（2026-09-26）

## 当前预览

- 前端：<http://localhost:3000/workspace/strategic-map>，Vite 3000 → Python 1604。
- Python 使用 `runtime/integration-preview-20260925` 中现有预览库；没有用源数据库覆盖项目数据库。
- 图谱读取 `runtime/hyper-fusion-20260926/graphs` 的对账副本；定时扫描、启动付费探针保持关闭。
- 可用 `scripts/start-fusion-preview.ps1` 检查/启动同一配置；`-Restart` 先检查是否存在运行中的调查任务，再重启指定 1604 服务。
- 原有未提交改动保留，没有 reset、clean、提交或批量覆盖工作树。

## 最新截图反馈：已修复

1. **右侧 AI 能力、科学能力太高**：移除两张大卡片，改为两列紧凑横排。保留独立下拉框、人工修改标记、AI 依据折叠展示。未初评不重复占用一行。
   - 有人工标记时内容区高约 **39 px**；编辑时桌面 **45 px**、390 px 手机 **50 px**。
   - 保存后中间列表和右侧同步；完整刷新后仍保留。修改能力不改变团队证据评分。
   - 核心方向仍为三行省略，团队详情保留全文。
2. **关系图谱 0 节点、Not Found**：前端已热更新到 `/integration/graph`，旧 Python 进程尚未装载该路由，产生 404。已重启到对应完整版本并固定预览启动配置。
   - 接口故障现在显示“图谱接口暂不可用”及重试按钮，不同时显示“没有节点”。
   - 浏览器验证故障注入 → 重试 → 图谱恢复；正常页面有真实 Canvas 节点和边。

主要代码：`ui/src/pages/StrategicMap/TeamJudgementEditor.tsx`、`GraphWorkspace.tsx`、`FusionGraphWorkspace.tsx`；后端 `ai4s-tool/ai4s_tool/api/research_fusion.py`、`api/__init__.py`。

## 两个 Hyper-Extract 项目的实际接入

| 源项目 / 代码 | 复用及映射 | 边界 |
| --- | --- | --- |
| `E:/web/lab/new/Hyper-Extract/hyperextract/triage/ingest.py` | 原始主体、事件、来源进入隔离分诊库 | 机构、作者保持各自身份，不自动成为团队 |
| 同目录 `tree.py`、`config.py`、`stability.py` | L1→L3 主题树、来源 A/B/C、三维历史、EMA/滞回的期次展示 | 不把来源阈值当作 AI4S 已校准标准；与团队评分、任务分独立 |
| 同目录 `daily.py` | 确定性事件/类目/评分摘要；融合接口增加已核团队和推荐变化 | 来源历史按事件日期回看，不算作导入当天的新成果 |
| `E:/web/lab/Hyper-Extract-1/Hyper-Extract/ZN`、`GW` | 国内/国外图谱 JSON、关系及现有索引复制到新目录，原生 React/ECharts 展示 | 未嵌入静态源页面；源节点不声称都是已审核团队 |
| 第二个项目 `daily-hier/scan.py` | 独立本机 1606 适配器接入原候选发现、模型研判和任务进度 | 只在显式开始扫描时调用模型；本轮没有执行真实扫描 |

### 同页功能

- **关系图谱**默认展示来源知识节点与已核团队证据，另可切换仅看已核团队证据。
- 领域、子领域继续筛选当前视图。无审核映射的来源 L3 不按相似名称自动塞入 AI4S 子领域。
- 机构名称精确相同时只增加“同名机构”关系；不会把机构事件自动计到下属团队。
- **动态情报**恢复 A/B/C 筛选、关注筛选、三维评语、历史曲线、原文链接、来源 L1→L3 树、类目审核与单项撤销、按日期的每日报告。
- 正式机构榜仍只收已核中国内地科研机构。当前有评分记录且通过资格审核的是 **4 个机构**，不能把全部 208 个来源主体都叫科研机构。
- 来源专题扫描使用“机构与作者”名称、原来源分数/排序及三维理由。原七维团队准入分不作为来源机构影响力分；候选上限按源引擎实际最大 30 展示。
- 浏览器图谱搜索使用已加载节点进行本地匹配，不因搜索框输入调用付费模型。

### 数据对账

`ai4s-tool/scripts/reconcile_hyper_graph.py`：只允许创建新目标；检查重复 ID、悬空边、逐文件 SHA256，拒绝覆盖已有目录。

| 数据 | 核对结果 |
| --- | --- |
| 国内来源图谱 | 1179 节点、1481 边；更新时间 2026-09-22 |
| 国外来源图谱 | 244 节点、315 边；更新时间 2026-09-21 |
| 对账副本 | 22 文件；0 重复节点 ID、0 悬空边；源文件与副本逐项哈希一致 |
| 分诊库 | 208 来源主体、460 事件、281 来源评分期次 |
| 团队库 | 116 支已核团队/科研单元，6 大领域、25 子领域 |

第二个仓库的 ZN/GW 核心图谱与先前旧路径快照哈希相同，因此没有把同一份知识图谱重复导入。1423 个图谱节点包含领域、机构、作者、事件等，不能与 116 支已核团队直接比较。

融合图谱按大领域读取的实际节点/边数（国内、对应高原/高峰）：

| 领域 | 节点 | 边 |
| --- | ---: | ---: |
| 科学通用底座 | 214 | 252 |
| 通用 AI | 1007 | 1213 |
| 高能物理与量子科技 | 138 | 150 |
| 化学与材料 | 181 | 202 |
| 生命科学与医学 | 250 | 301 |
| 地球科学 | 176 | 199 |

领域间可共享来源机构，不应把上表简单相加当成去重总数。

## 验证记录

- 源分诊 `stability / daily / tree_growth / ingest` 离线测试：**89 passed**，模型为测试替身。
- 本项目融合、原图谱、分诊合同测试：**34 passed，4 subtests passed**。
- 前端图谱、主页面与聚合测试：**8 passed**；`tsc -b` 通过。
- 浏览器 1440 / 1366 / 390：关系图谱真实渲染、两种图谱视图、国内/国外、404 故障与重试、正式机构榜、历史曲线、类目事件、历史日报、无页面横向溢出。
- 能力编辑在独立副本 1605 验证：两个下拉框、列表/详情同步、保存刷新、人工来源、评分不变、方向三行截断与详情全文。
- 来源扫描结果在浏览器拦截 API：机构来源分 72.5 不被团队准入 0 分覆盖。真实模型任务没有启动；1606 的 `jobs` 为 0。
- 日报接口测试重复请求相同哈希；来源正文版本和分数变化会改变哈希；L3 通过父 L2 + 标签正确取事件，不混入其他主题。
- 测试写入只在 `runtime/hyper-fusion-20260926/strategic_map.db` 副本；图谱浏览器验收拦截所有工具 API 写操作。实际图谱、排行榜、日报检查为只读。

验收脚本：`ui/scripts/verify-research-fusion-browser.mjs`、`verify-strategic-team-edit-browser.mjs`；新增后端测试：`ai4s-tool/tests/test_research_fusion.py`。

## 尚未验证、尚未启用

- 真实付费专题扫描的供应商可用性、结果质量与费用没有验证。本轮只验证源引擎能在禁网初始化条件下装载现有索引，API 元数据与前端适配正常。
- 定时 RSS/检索增量、自动付费日更没有启用。用户设定的未来无日费用上限不等于本轮自动执行授权。
- 来源 A/B/C、三维权重、EMA、异动阈值仍需跨六领域样本校准；页面保留“来源评分待校准”，没有批量填成高分。
- 合并身份的历史重评仍保留原始期次与独立修订；没有用同名机构连接代替审核合并。
- 融合日报当前是可重复的输入哈希回放；`frozen` 只描述已有团队/推荐快照。尚不是把整套来源图谱、分诊、团队都固化后的新每日发布流水线。
- 类目单项审核/撤销的后端已有测试；此次浏览器未在真实数据库执行审核写操作。
- 本轮没有验证新热点研报、专家辩论或海报生成，不能据此声称已满足老师所有研判系统要求。

## 回退与运行

- 图谱页面可随时切回“已核实团队证据”。新增融合接口为读取投影；停用融合路由不会删除原始图谱、分诊、团队记录。
- 快照副本和清单保存在 `runtime/hyper-fusion-20260926/graphs`；原 ZN/GW 与原项目数据库保留。
- 主预览后端：`./scripts/start-fusion-preview.ps1`；前端仍使用已运行的 3000。
- 专题扫描适配器运行命令（显式启动服务不会执行扫描）：

```powershell
$env:TIKTOKEN_CACHE_DIR='E:/web/lab/ai-agent/runtime/hyper-fusion-20260926/tokenizer-cache'
& 'E:/web/lab/new/Hyper-Extract/.venv/Scripts/python.exe' E:/web/lab/ai-agent/ai4s-tool/scripts/serve_hyper_scan.py --source E:/web/lab/Hyper-Extract-1/Hyper-Extract --snapshots E:/web/lab/ai-agent/runtime/hyper-fusion-20260926/graphs --port 1606
```

本轮界面修复没有必须用户再决定的选项。校准与正式启用定时日更应在真实来源质量验收后单独交付，不混入本次“已通过”的界面结果。
