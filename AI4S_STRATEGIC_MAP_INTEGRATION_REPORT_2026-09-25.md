# AI4S 战略图谱同页融合：实施、对账与后续优化

> 2026-09-26 已按用户要求改为白色主题和仅展示审核通过资料；当前展示边界及老师要求核对见 [最新核对报告](AI4S_TEACHER_REQUIREMENTS_REVIEW_2026-09-26.md)。本文件保留 9 月 25 日实施状态。

日期：2026-09-25。以本文件为本次实施的最新状态；此前的《主体影响力融合与优化计划》记录了源系统分析和当时尚未完成的计划，不能替代本次验收结论。

## 1. 当前可用入口与隔离边界

- 本地入口：`http://127.0.0.1:3000/workspace/strategic-map`。当前 3000 Vite 代理到新 Python 工具进程 1604；Java 8100、MySQL 3306 与原 1601 进程未切换。3003 仍可作为完整隔离预览。浏览器内旧影响力地址跳转至战略图谱的“动态情报”视图。
- 1604 显式使用 `ENV=preview`、`STRATEGIC_MAP_SCHEDULER_ENABLED=false`、`AI4S_SKIP_EMBEDDING_HEALTH=1`，分别连接 `runtime/integration-preview-20260925/strategic_map.db` 与 `impact_triage.db` 两份副本。原 `ai4s-tool/strategic_map.db`、`impact_triage.db` 未执行本次迁移，也没有运行付费扫描。
- 当前是本机可访问的融合版，**不是公共生产部署**。审核写接口只应在受控环境使用；公网发布前需结合现有账号体系确认操作权限、审计操作者和 CSRF/鉴权边界。

## 2. 源系统与 AI4S 的对象和数据映射

| 源系统事实与代码 | AI4S 处理 | 已实现的防混淆规则 |
|---|---|---|
| Hyper-Extract `hyperextract/triage/ingest.py` 的实体是机构或作者，事件和多个原文来源分开写入 | `impact_entity`、`impact_event`、`impact_event_source` 保留来源主体 ID；现有 `strategic_map_team` 和人员 ID 原样保留 | 机构事件绝不自动成为任何下属团队成果。机构—团队关系要凭具体官网页面审定，写 `impact_team_link` 并留下审核/撤销记录；本副本实际关联 **0 条**。 |
| 源 L1 高原/高峰、六个 L2、20 个 L3；`hyperextract/triage/tree.py` 维护候选、审核和回滚 | L2 映射到 AI4S 六大领域；源 L3 只在情报树展示 | L3 不自动对应 AI4S 的 25 个子领域。同名“具身智能”事件归到现有 L3，未生出重复节点；审核变更可单项撤销。源候选阈值暂仅作提示，未当作 AI4S 自动生长标准。 |
| 源 `scoring.py` / `stability.py` 用三维来源分、EMA、A/B/C 与降级滞回；现团队另有 `hybrid-evidence-v3` | 来源评分期次原样保存；新增 `impact_score_revision` 允许同日多版本重评。任务匹配为 `MATCH_VERSION` 的第三套分数 | 三分独立展示，不相加。旧 A/B/C 明示“来源分·待校准”；未校准正式榜按机构名称排列。只有标为 `calibrated` 且 `active=1` 的新修订版才接管生效排序；原 281 期始终留存。 |
| 源 `daily.py` 在固定上游输入后确定性组装日报 | `strategic_intelligence_daily` 存日期、输入版本/哈希、修订号和正文 | 固定事件、审核、评分和团队推荐快照生成“流入→审核→机构榜变化→团队推荐变化→六领域摘要→跟进清单”；同样输入重放不新增版本，迟到证据生成修订版。 |

主体资格按中国**内地科研机构**执行：官方一手来源、内地位置、明确的大学/科研院所/独立企业研究机构类型均需审核。作者、产品品牌、社群、协会、执法单位留在参考档案；港澳机构不自动入正式榜。当前仅中科院、清华、复旦、浙大四个机构资格样本通过；这不等于其旧分数已经通过校准。

智谱、Z.ai、Zhipu AI 作为一个展示身份，原实体、事件和评分期次仍保留。身份归一后的历史重评按当时可见事件建立 3 条**待校准证据修订版**，不把三个主体旧分数相加，也不伪造当年的重新定级。单项身份链接可撤销；详情展示原分与修订版的来源关系。

## 3. 已落地的同页功能

1. `ui/src/pages/StrategicMap/index.tsx` 以同一路由提供“任务推荐、团队库、关系图谱、动态情报”四视图，共用领域/子领域导航。任务页默认 N=10，可选 5/20；卡片分别显示任务匹配、团队原分与机构影响力状态，提供能力、原文、缺口和下一步。桌面首屏强调可用结果，手机信息完整。旧团队筛选和图谱保留；`VITE_STRATEGIC_MAP_FUSION_ENABLED=false` 可回到旧双视图。
2. `ai4s_tool/api/task_recommendations.py` 的 `POST /v1/strategic-map/task-recommendations` 只用本地索引与现有团队核验记录，不发付费请求；`GET /.../{runId}` 回看结果；显式 `POST /.../{runId}/expand` 才请求后台全网扫描/领域刷新，并返回可轮询进度。统一分页检索覆盖团队、别名、已索引成果、机构事件和来源，返回命中片段。远端扫描未连通时扩展接口会说明原因。
3. 正式推荐要求国内团队、既有人员门槛、最近已验证的团队研究记录，以及该记录中机构名称、能力和成果的有效原文引证；同条团队记录至少有成果证据。这里的团队研究记录核验**不等于**跨系统 `impact_team_link` 已审核，本副本的跨系统关联仍为 0。当前索引 **14 支正式候选团队、73 条引证声明**。这属于已有研究记录的结构化证据，仍待对五个跨领域任务逐条人工核查原文与结论。其他团队只出现在“待核线索”，不填充 N 的缺口。
4. `ui/src/pages/ImpactTriage/index.tsx` 内嵌动态情报：正式机构榜与参考档案、L1→L3 审核树、主体与原始来源、跨期期次、机构—团队关系审核、日报及修订版本。入库入口要求原文 URL、摘录、内容 SHA-256 和审核通过的主体/方向；同 URL 同内容幂等，同 URL 新内容产生事件版本。人工入库开关默认关闭。
5. 任务匹配先使用可解释的方向词与团队引文覆盖；跨领域人工样本建立前，不宣称它是已校准的质量指标。请求重试上限、初始并发 4、429/5xx 退避与连续故障熔断已接入现有研究调用；`.env_template` 不设每日费用上限，可配置单价用于费用估算，但目前未启用自动付费日更。

主要实现文件：`ai4s-tool/ai4s_tool/api/impact_store.py`、`impact_triage.py`、`task_recommendations.py`、`strategic_map.py`、`domain_research.py`、`team_research.py`；前端 `ui/src/pages/StrategicMap/RecommendationWorkspace.tsx`、`ui/src/pages/ImpactTriage/index.tsx`；迁移/对账脚本在 `ai4s-tool/scripts/`。

## 4. 副本迁移对账

`scripts/reconcile_integrated_preview.py` 在最终代码/服务启动后复跑结果 `ok=true`：

| 表或指标 | 原库 | 隔离副本 | 核对结果 |
|---|---:|---:|---|
| 战略图谱团队（含软删） | 216 | 216 | 稳定 ID、原始行字段一致；活跃 186 |
| 战略图谱人员 | 478 | 478 | 稳定 ID、原始行字段一致 |
| 分诊主体 / 类目 | 208 / 28 | 208 / 28 | 原始 ID 一致 |
| 分诊事件 / 来源 | 460 / 465 | 460 / 465 | 原始 ID 一致 |
| 分诊来源评分期次 | 281 | 281 | 原始 ID 和旧期次未覆盖 |
| 新增的团队成果引证 / 身份重评修订 / 冻结日报修订 | 0 | 73 / 3 / 3 | 仅写副本 |
| 机构—团队审核关联 | 0 | 0 | 没有足够的具体团队归属依据，未自动建立 |

两库的 `PRAGMA foreign_key_check` 都为 0，`integrity_check` 均为 `ok`。迁移脚本默认干跑，正式写入副本前做 SQLite backup；副本的备份保存在 `runtime/integration-preview-20260925/`。对账只承诺上述原有表/字段和来源 ID；本地浏览器/性能测量产生的推荐任务快照会继续增加。

## 5. 验收证据

- Python 指定回归 `pytest tests/test_impact_triage.py tests/test_task_recommendations.py tests/test_strategic_map_product_rules.py tests/test_strategic_graph.py tests/test_domain_research.py -q`：**94 passed，4 subtests passed**。覆盖主体资格、团队隔离、身份及同名类目、同日评分修订、生效版本读取、事件幂等、日报重放、回滚、失败重试与推荐规则。
- TypeScript `npx tsc --noEmit` 和 `npm run build` 成功；生产包仍有主 JS chunk 约 6.84 MB（gzip 1.94 MB）的体积警告，需要在后续前端性能工作中拆包。
- 3000 实际浏览器验收：1440×900、1366×768、390×844 覆盖任务、引文、检索、类目、日报、旧团队和图谱；三队比较用响应夹具检查，未向库中写入假团队。脚本 `ui/scripts/verify-strategic-integration-browser.mjs` 拦截付费扩展/刷新/扫描；有截图留在 `E:/Codex/visualizations/2026/09/25/01a0d855-6dd1-7131-86b7-b044a8713404/`。
- 本机副本量级，30 次本地检索 p95 约 **29.5 ms**，12 次缓存任务推荐 p95 约 **96.1 ms**，一次冷启动推荐约 **720 ms**。只说明此机器及当前数据；不代表公网网络时延或未来规模。
- 真实数据任务样例有缺口：具身智能 2 正式/1 待核、多模态大模型 2/3、药物分子设计 0/2、高温合金 1/0、地理空间数据分析 1/1、量子计算 0/1。页面显示实际短缺，没有把未核线索排入正式推荐。

## 6. 尚未验证和下阶段优化顺序

1. **人工质量验收**：选择至少五个跨领域任务，对候选团队逐条打开原文，记录是否确证团队归属、具体成果、日期和任务相关性；修正误引用和覆盖不足，冻结一组任务匹配校准样本。当前自动化只核对结构化引文存在，不能代替教师/领域专家核实。
2. **机构 A/B/C 校准**：至少 30 个跨领域中国内地科研机构，人工标注三维分及来源可靠性，回放 0.4/0.3/0.3、EMA、异动和降级滞回；记录误报漏报、规则版本和生效日期，通过验收后才写 `calibrated` 且 `active=1` 的修订版。现有 3 条历史身份修订没有新等级，正式榜旧等级始终标来源待校准。
3. **类目生长阈值**：按去重成果、独立发布方、持续天数和语义冲突建立跨领域样本，校准候选展示与审核阈值；L3 审核后也不自动覆盖战略图谱子领域。继续补具体机构—团队官方关系，记录审核与撤销；不借机构的事件提升下属团队评分。
4. **增量日更**：配置可靠来源采集、发现日期与事件日期、内容哈希版本和迟到证据补抓；先沙盒回放、再小批量付费验收，检查调用、估算费用、重试/熔断及日报修订。之后才打开定时任务。当前没有真实每日全网召回率、供应商账单和自动扫描质量的验收结论；“无日费用上限”仅是预算策略，非无限重试。
5. **发布质量**：公网使用前补审核接口权限及操作者审计；前端主包拆分，实测弱网首屏；在目标部署上复测 p95、异常/空结果、原文失效、浏览器尺寸及旧路由。当前 3000 是本机融合预览，不宣称上线。

## 7. 回滚

1. 前端启动/构建设 `VITE_STRATEGIC_MAP_FUSION_ENABLED=false`，即可显示旧团队库/关系图谱；无需丢弃源数据。
2. 3000 Vite 的 `AI4S_TOOL_BASE_URL` 改回原 1601，并重启前端；1601 进程和原库未变。1604 可停止，隔离副本可保留复核。不要删除未提交文件或覆写两个原库。
3. 类目、身份、机构—团队关系按各自审计 ID 调用撤销接口；整库恢复只对副本使用事前 `.bak`，恢复前先停止 1604。原有 98 项未提交改动在实施前已存在，未做 reset/clean/覆盖；本次变更同样仍未提交，需按实际 diff 审阅。
