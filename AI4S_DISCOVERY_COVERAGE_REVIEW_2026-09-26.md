# 师兄项目检索覆盖与融合修复（2026-09-26）

> 后续进展：本文件保留上一批次的 86 支/14 支基线。最新官方目录扩充、资格规则调整、116 支公开团队及 64 支成果推荐池，见 [官方覆盖扩充验收](AI4S_OFFICIAL_COVERAGE_EXPANSION_2026-09-26.md)。以下“继续保留人数/评分门槛”等描述已被新版本替代。

## 结论

此前融合确实存在展示与数据接入错误，不能全部归因于核验严格。

1. 团队库、关系图谱、公开搜索错误地共用了任务推荐门槛：负责人、至少两名成员、评分和成果必须同时齐全，否则整支团队消失。
2. 接口只读取 `verified` 调研记录，漏掉了 `official_directory` 官方目录核验渠道。存量有 75 份此类记录。
3. 单领域同步前调用全库人员补全，而且没有传递领域、子领域和时间预算。已有 5,554 条补全历史；159 个调查工作项标为 `budget_exhausted`。这支持存在重复补全与积压，但不能据此断言每个超时都由这一个原因造成。
4. 新版 Hyper 分诊库已被导入，但未接入团队发现入口；原入口依赖旧图谱服务/快照。新版 `new/Hyper-Extract` 本身没有旧版 `ZN/data.json`，不能把两个版本的接口视为相同。

本轮已修复上述四处。**恢复展示 86 支有归属与团队证据的国内科研团队、344 条引文；其中满足完整任务推荐门槛的仍为 14 支。** 这是恢复已有证据的展示，没有把未核线索改成已核，也没有执行新的付费检索。

## 一、源系统为什么看起来搜得多

以下是本机文件和 SQLite 的只读统计，不是 README 宣传数字。

| 数据层 | 本机数量 | 口径 |
|---|---:|---|
| `dates/fetch-*.json` | 41 个文件、24,296 条 | 2026-08-12 至 09-21 的跨期资讯积累 |
| 原始链接 | 23,528 个去重 URL | 不等于团队数量 |
| 来源标签 | 332 个 | 标签数量不等于有效、独立、官方来源数 |
| 国内/国外切分 | 721 / 7,090 条 | 上游评分与分类后的归档 |
| 当前分诊主体 | 208 个 | 199 个标为机构、9 个作者；没有团队表 |
| 当前分诊事件 | 460 条 | 日期范围 08-12 至 09-10，尚未覆盖全部归档日期 |
| 来源/评分期次 | 465 / 281 | 每个主体可以积累多事件、多期评分 |

源注册表的“机构”里实际有 Manus、vivo、天猫 AI 助手、北京正负电子对撞机等不同主体类型；国内分类里也存在需要进一步核对的境外作者/港澳机构。它的 199 不能直接解释成 199 支合格国内科研团队。

### 真正值得学习的代码链路

- `hyperextract/triage/fetch_split.py`：评分过滤和国内/国外分类共用实现、分类结果落盘缓存。同一批输入可重放，避免重复模型调用。
- `hyperextract/triage/ingest.py:425`：每日按资讯条目增量入库，先别名、注册名匹配，再让模型处理未命中主体；累计事件和原文来源。
- `hyperextract/triage/recall.py:104`：按方向普查已有主体，不设普查上限。
- `hyperextract/triage/recall.py:139`：方向与关键词通道加向量召回；这里是**库内召回**，不是现场全网搜索。
- `hyperextract/triage/recall.py:288`：宽召回后才把 30～50 个候选送入贵的评分步骤。精排截断不应让团队目录本身消失。
- `hyperextract/triage/runner.py`：对受影响主体增量复评，避免每条新闻都重扫全库。
- 配套 `E:/web/lab/ai4s-daily/src/fetcher.py`、`src/search_fetcher.py` 包含 RSS 并发抓取及 Tavily 检索入口；本轮未启动它们。不能只融合静态排名页而忽略这种持续积累方式。

旧版 `E:/web/lab/Hyper-Extract-1/Hyper-Extract/ZN/data.json` 另有 1,179 个节点，其中 781 个事件、320 个机构、69 个作者、9 个类目节点。它与本次指定的新版分诊库是不同数据集，不能把所有图节点都数作团队。

## 二、已落地的修复

### 1. 已核团队目录与任务推荐分开

新增 `ai4s-tool/ai4s_tool/api/verified_team_catalogue.py`：

- 接受已发布的官方目录记录与独立审核记录；校验稳定团队身份、来源、目录内容签名、国内归属和领域依据。
- 有身份冲突、撤销或归属问题的记录不进入公开目录；作者、机构整体、产品不自动变成团队。
- 展示简介、方向、引用取核验记录，不用未经审核的旧简介补空。
- 官方“研究成果”专门段落可作为成果证据，一般个人简介不会因为包含“模型”等词就被自动算成成果。
- 只生成读取投影，不更改原团队核验状态、分数或数据库结构。

`task_recommendations.py:128` 建立统一目录读取；团队库、关系图谱和公开搜索共用它。
`task_recommendations.py:168` 的任务推荐继续单独要求原人员/评分门槛与本团队成果。

| 领域 | 库内存量 | 公开目录 | 完整推荐门槛 |
|---|---:|---:|---:|
| 科学通用底座 | 22 | 6 | 3 |
| 通用 AI | 29 | 10 | 5 |
| 高能物理与量子科技 | 15 | 3 | 1 |
| 化学与材料 | 35 | 21 | 1 |
| 生命科学与医学 | 69 | 41 | 1 |
| 地球科学 | 16 | 5 | 3 |
| 合计 | 186 | **86** | **14** |

86 支中 59 支以官方目录作为当前展示依据，27 支以独立审核作为依据。其余记录仍保存在库内，未删数据。完整推荐门槛数量不是任意任务的匹配数量，具体任务仍会进一步筛选。

前端延续白色主题，团队名称放在首行、所属机构放在次行；页头显示总团队数。改正“所有目录记录都有完整成果审核”的过度表述。详情页使用同一份证据投影。

### 2. 减少无效补全

`team_enrichment.py:532`、`:708` 接受领域/子领域，避免单领域更新扫描所有团队。
`domain_research.py:827` 给补全步骤预留时间额度，保证后续发现和逐队审核有执行机会。
相同团队上下文的补全尝试保留 12 小时缓存，减少缺少成员名录时反复补查。时间额度限制在团队边界检查，单次正在执行的网络请求仍由原有超时机制约束。

### 3. 接上源分诊机构到团队发现

新增 `triage_discovery.py`，只读已导入分诊库，按当前领域与子领域证据选出研究机构检索入口，再进入现有具体团队命名和原文核验流程。显式排除作者及港澳台名称；旧图谱仍可作为补充来源。

该入口是后台发现用途，不直接给机构入榜资格，不把机构事件当下属团队成果。取消领域调查调用旧图谱时只取前 16 家机构的限制，仍保留每轮查询、时间与并发控制。没有自动启动扫描。

## 三、验收与运行

- 预览：`http://localhost:3000/workspace/strategic-map`，Python 使用隔离库的 1604 服务。
- 师兄项目入库、召回、切分、日报测试：105 项通过。
- AI4S 目录、推荐、领域调查、图谱、官方目录测试：81 项通过，另有 4 个子测试；随后新增的领域隔离/时间预算回归测试也通过。
- 前端相关 4 文件、6 项单测通过；TypeScript 类型检查通过。
- 浏览器 1440×900、390×900 通过：推荐、引用检索、动态情报、分类、日报、团队详情、关系图谱；桌面另逐领域对照 API 与可见行数，确认共 86 支。没有触发付费扫描。
- 公开图谱 450 个节点，包括团队、机构、领域和证据；这 450 也不能写成团队数。
- 本地热缓存搜索 20 次，p95 约 71 ms；目录首次构建约 482 ms。不同硬件与更大数据量需重新测量，不代表互联网检索速度。

机器可读逐队对账：`runtime/integration-preview-20260925/discovery-coverage-20260926.json`。
复跑工具：`ai4s-tool/scripts/audit_discovery_coverage.py`，默认只读、无模型调用。
截图：`E:/Codex/visualizations/2026/09/25/01a0d855-6dd1-7131-86b7-b044a8713404/strategic-catalogue-86.png`。

启动前核实没有活动领域/图谱扫描，随后更新隔离后端；未覆盖原库、未修改 Hyper 源库、未重置既有未提交改动。本轮没有需要撤销的业务数据迁移。代码回退应只撤销本轮补丁，禁止整体 reset；可用既有融合开关回到原视图。

## 四、仍未达到老师要求的部分及继续优化顺序

**本轮解决了“已有核验数据被藏起来”和两处发现流程问题，没有完成全国团队覆盖。**

1. **优先补齐低覆盖领域的官方目录。** 当前固定适配目录只有 5 个，集中在清华、中科院生物物理所/遗传所/金属所、上交；量子和地球科学缺少同等完整的目录入口。以机构覆盖数、具体团队数、原文成果数分别衡量扩充，避免只在一个大所堆数量。
2. **积累新闻发现与官方团队名录两条输入。** 源归档 09-11 至 09-21 尚未全部进分诊；应通过批次、原 URL/内容版本去重、可重放入库接入，而不是重复抓取历史资讯。涉及模型的入库仍需按既有“不启动付费扫描”约束执行。
3. **把已核团队变成可推荐团队。** 逐领域优先补当前任务所缺的负责人、成员归属和本团队成果，保留完成记录与失败原因；不能为达到 N 而删除全部资格要求。
4. **任务检索的语义与证据校准。** 当前本地匹配仍以可解释文本匹配为主；源系统的同义词与双通道召回值得进一步接入。采用六领域任务集测真实命中、误召回和原文支持率，不直接照搬 source A/B/C 阈值。
5. **日更后重新计算任务推荐并说明变化。** 当前日报/机构榜基础仍在，但主动日更、历史版本评分校准、完整任务重排闭环尚未因此完成。

这几项不需要新的界面偏好决策；付费扫描和定时任务本轮保持未启动。尚未验证新的发现入口实际能新增多少合格团队，不能承诺下一轮一定补满每领域 N=10。
