# 白色主题改版与老师要求核对（2026-09-26）

> 后续修正：本文件记录的是当日上午的版本。之后发现目录误用了推荐门槛、漏接官方目录证据，现已修复为 86 支公开目录团队、344 条引文；任务推荐独立保留完整门槛。当前统计、代码依据与验收见 [检索覆盖与融合修复](AI4S_DISCOVERY_COVERAGE_REVIEW_2026-09-26.md)。以下 14 支的旧展示口径不再用于团队库。

## 结论

目前是**任务选队与证据展示可用，但数据覆盖和持续更新尚未达标**的本地版本。老师第一项要求已形成工作流程，不能据此声称推荐质量经过了完整验收；第二项“热点动态更新与国内团队推荐”还没有完成自动更新闭环。

本次按用户要求调整了展示口径：展示页只使用现有审核记录通过、符合人员门槛且有成果原文的团队，不再展示未核线索。没有把原库的其他团队或来源系统的旧分数改成“已核验”。**系统原有审核状态不等于本轮又完成人工逐条复核**；原文是否充分支持能力、具体任务是否适用，仍需要有领域知识的人工样本验收。

## 已修改并运行的页面

- `http://127.0.0.1:3000/workspace/strategic-map`：白色顶部栏、白色任务输入区与卡片、浅灰页面底色，蓝色仅用于按钮、选中态和链接。领域名称完整换行，手机顶部标签减少横向滚动。
- 推荐卡片保留任务匹配、团队评分、原文证据、适用范围及下一步；去掉未审核线索、尚未建立的机构关联提示和未校准的影响力数值。三队比较保留。
- 团队库与默认检索共用同一套成果审核门槛。团队详情只展示已发布团队，并过滤没有显式审核状态或来源的人员；已有记录不删除。异常时不回退展示未经审核的资料。
- 关系图只从当前审核通过的团队、机构归属记录和引文构建；原文节点可以下钻。未审核的源系统底图、扫描候选和模型即时对话结果不混入这个展示视图。原管理代码仍保留。
- 动态情报展示官网依据可查的国内科研机构、战略图谱现有类目及过滤后的团队日报。来源树的 28 个节点仍为 `source_unreviewed`，没有将它们批量变成审核通过；原 L1→L3 审核代码保留。当前机构区按名称列档案，没有冒充校准后的影响力排名。

实现依据：

- `ui/src/pages/StrategicMap/RecommendationWorkspace.tsx`：白色任务页、推荐依据和比较。
- `ui/src/pages/StrategicMap/index.tsx` 的 `loadVerifiedSnapshot`：初始加载和刷新均按审核目录过滤。
- `ui/src/pages/ImpactTriage/VerifiedIntelligence.tsx`：仅发布已核资格的机构信息，屏蔽源分和审核候选。
- `ai4s-tool/ai4s_tool/api/task_recommendations.py` 的 `_candidate_evidence`、`verified_teams`、`verified_graph`、`verified_daily`：共用证据口径，默认检索不读未经审核的档案；历史推荐投影不再返回 `pendingLeads`。

## 逐项对照老师的要求

| 老师的要求 | 当前判断 | 代码/数据依据与具体缺口 |
|---|---|---|
| 输入领域或任务，输出 N 支国内团队 | 部分满足 | 有任务输入、六领域过滤、5/10/20 数量选择、保存推荐运行。实际仅 14 支团队通过当前成果证据门槛，部分领域无法返回一支，更不能保证每个任务都有 N 支。 |
| 区分机构、团队和个人，统一名称与编号 | 基础满足，关联仍不完整 | 稳定 ID 分开保存；智谱/Z.ai 的展示身份已统一，不把机构事件摊给团队。但跨系统 `impact_team_link` 实际为 0，不能宣称机构影响力已经关联到每支团队。 |
| 单独展示任务匹配度，允许团队比较 | 功能满足，评分效果未验收 | 已有独立匹配分和三队比较；`_score` 当前主要用词项/引文覆盖计算，不是经过跨领域专家样本校准的任务能力评估。100 分不能解释为确定具备交付能力。 |
| 每队说明能承担什么、有哪些成果证明、还缺哪些信息、如何进一步确认 | 部分满足 | 能力和引文可展示，原文链接可打开；目前“适用范围”和后续步骤仍偏通用，没有逐任务拆解硬件、人员、交付周期等限制。隐藏未核线索并不会让这些问题自动完成核验。 |
| 数据、证据、团队档案与研判结果分层保存 | 基础满足 | 团队主档案、研究记录、成果引证、推荐运行、机构事件和评分版本分开保存；尚缺五个跨领域任务的人工逐条原文核查。 |
| 新证据进入后更新团队与推荐名单，并记录变化原因 | 未完成闭环 | 有显式深搜入口、团队研究作业和结果保存；正在查看的旧推荐不会在新证据落库后自动重新计算，日常自动扫描仍关闭。变化原因主要是通用文字，尚未逐条说明是哪份证据改变了哪项判断。 |
| 围绕领域生成变化摘要和跟进清单 | 框架满足，内容能力不足 | 已有固定输入的日报及发布过滤；摘要主要是更新/名单数量，跟进项偏模板。尚不能视作老师需要的高质量热点综合研判。 |

## 当前数据与任务实测

2026-09-26 本地 API：底层保存 186 支活跃团队；36 支满足原候选门槛；14 支符合当前成果证据发布门槛，合计 73 条引证声明。机构资格档案 4 个，实际跨系统机构—团队关联 0。数目较少是审核口径收紧后的结果，不用未核记录填充页面。

本地请求均未调用扩展调查；目标 N=10：

| 任务 | 当前返回团队 | 未核线索 |
|---|---:|---:|
| 科学计算与仿真 | 4 | 0 |
| 具身智能 | 2 | 0 |
| 量子计算 | 0 | 0 |
| 高温合金 | 1 | 0 |
| 药物分子设计 | 0 | 0 |
| 地理空间数据分析 | 1 | 0 |

这些是功能和覆盖检查，不是六个任务的人工推荐质量认证。最应优先补的是原文证据、任务匹配质量与自动更新闭环，而不是继续增加展示数量。

## 后续优先顺序

1. 为以上六类任务逐项明确必须能力、成果及交付约束；每个任务至少核对 5 支团队，记录原文支持与不支持的判断，优先填补量子/生命科学的零结果。
2. 将任务匹配拆成可解释的能力维度，并用人工核验样本校准；为每个推荐给出任务特有的限制、反证和下一步，不能用统一模板替代。
3. 接通“新证据入库→团队档案审核发布→受影响任务重新推荐→证据级变化说明→日报修订”，先回放再小批量验证。
4. 完成机构评分与类目阈值校准，再发布有明确版本和证据的影响力排序；在此之前仅展示机构身份与官网依据。

## 验收与运行记录

- Python 分诊/推荐指定测试 15 项通过；新增检查覆盖默认搜索不泄漏原始未核档案、资料撤回后不被旧索引重新发布、关系图和日报的发布过滤。
- 前端 10 项测试通过，覆盖旧推荐线索隔离、人员审核和来源门槛等；桌面 1440、笔记本 1366、手机 390 像素浏览器检查通过，团队详情与原文下钻入口可用。三队比较用响应夹具验证，不写入假团队。
- TypeScript 与生产构建通过；原有全站主 JS 包仍约 6.84 MB（gzip 1.94 MB），公网弱网首屏还需要单独拆包与实测。本地接口快并不代表弱网首屏已经达到要求。
- 仍连接 `runtime/integration-preview-20260925/` 两份数据库副本；未覆盖原数据库，未清理原有未提交改动；浏览器验收拦截付费扫描/扩展请求。
- 10:17 更新后端时中断了已在 10:02 启动的研究作业 `refresh_d1fbff806909446fa231d7e46124b3b0`。系统按原恢复机制将其标记为失败，逐队保存的增量结果保留。本轮没有自动重跑该付费调查，避免重复调用。
