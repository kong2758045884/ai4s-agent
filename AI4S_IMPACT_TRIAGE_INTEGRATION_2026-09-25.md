# 主体影响力分诊：代码核对、映射与分阶段集成

> 2026-09-25 后续已完成战略图谱同页融合；当前运行地址、对账和验收以 [最新实施报告](AI4S_STRATEGIC_MAP_INTEGRATION_REPORT_2026-09-25.md) 为准。

> 后续决策与实施状态以 [详细优化计划](AI4S_IMPACT_TRIAGE_OPTIMIZATION_PLAN_2026-09-25.md) 为准。下文“需要用户定夺”是当时的未决状态；用户现已授权按保守口径处理身份、资格、类目和重试，并暂定每日付费检索无费用上限。最新隔离库已将智谱/Z.ai/Zhipu AI 归为一个展示身份、将 20 条“具身智能”事件映射到原有 L3，并用官网核验四个国内科研机构样本。源库和战略地图团队分数未覆盖。

日期：2026-09-25。源仓库为 `../new/Hyper-Extract`。本文件记录已核对的事实与边界，不把源系统榜单或现有团队评分当作质量认证。

## 1. 交接文档与当前代码的差异

| 事项 | 代码和只读数据证据 | 结论 |
|---|---|---|
| 战略地图存储 | `ai4s-tool/ai4s_tool/api/strategic_map.py:39-93` 用 `STRATEGIC_MAP_DB_PATH`/`strategic_map.db` 创建 SQLite engine；团队、人物、领域分别在同文件 `:101-211` | 整体系统有 MySQL，但**战略地图业务表当前是 SQLite**。新分诊不迁入 MySQL 或改动现有战略地图表。 |
| 源 schema | `../new/Hyper-Extract/hyperextract/triage/db.py:26-137` 的 `SCHEMA_VERSION=5`，而源 `hyperextract/triage/__init__.py:11` 注释仍写“当前 2” | 以实际 schema 和测试为准，不能按模块注释判断。 |
| 方向归属 | 源 `db.py:54-60` 的 `entity_directions` 主键是 `(entity_id,direction_id)`；源 README 称“单一归属” | 实际是多对多：只读查询发现 22 个主体挂多个方向、最多 4 个，17 个主体无方向。跨方向榜单不能把行数直接当独立主体数。 |
| 前端展示 | 源 `scripts/visualize_dashboard.py:52-144` 读最近 `score_history`，`scripts/visualize_rank.py:145-164` 按 A→B→C 和同级分排序；HTML 由脚本生成 | 可复用查询和展示口径，不能直接复制静态 HTML 作为 React 集成。 |
| AI4S 团队分 | `strategic_map.py:457-513` 按团队资格、证据数量、人物等计算 `hybrid-evidence-v3`，不等于源三维主体影响力分 | 两套分数分表、分版本、分用途。不能用 A/B/C 覆盖团队 `score_total`。 |

## 2. 源链路与可复用机制

1. **事件入库**：`fetch_split.py:68-80,241-270` 对上游评分做硬过滤；`ingest.py:425-618` 复用 `ingest_entries`，先打 L2/L3/主体标签，再聚合同成果报道，把主事件写入 `events`，每篇报道写入 `event_sources`，记录主体与 L2 关系。`aliases.py:94-125` 用已沉淀别名匹配；`ingest.py:323-386` 还会消歧。复用“事件与来源分离、来源可回溯、显式别名表、幂等对账”的机制；上游 60 分硬门槛和模型的身份判断不能原样接入。
2. **类目生长**：`tree.py:103-200` 从无方向事件池聚类出待审 L2；`tree.py:545-645` 审核后回溯事件方向并补主体方向关系；`tree.py:694-762` 对同一 L2 下的 L3 标签按事件数生长，但事件仍锚在 L2。`tree.py:393-416,1406+` 用变更前快照回滚。复用“候选→审核→审计→回滚”与 L3 仅作组织层的边界；本次 AI4S 把 L3 固化改成人工审核，避免未经校准自动改树。
3. **排名**：`recall.py:139-216,288+` 双通道召回、去重并粗排截断；`evidence.py:47-145` 给主体组装分时段事件与来源；`scoring.py:35-93` 要求证据约束的成就/地位/趋势分；`runner.py:203-275` 写原始分、EMA 生效分、理由与期次。`stability.py:18-115` 负责 EMA、异动、A/B/C 门槛与连续降级。复用“历史期次、三维评语、证据链和等级变化解释”；当前只展示源分并标记 `uncalibrated`，没有自动 AI4S 复评。
4. **日报**：`daily.py:304-443` 的三段报告是纯输入组装；`daily.py:114-177` 的受影响主体增量评估有截断与跳过原因。日报文字的确定性依赖**已固定的输入**，上游 LLM 标注和评分并非确定性。本次 AI4S API 按已入库批次、审核和 AI4S 新评分记录确定性查询，源历史快照不冒充今天新增。

源 `pyproject.toml` 与仓库 `LICENSE` 均声明 Apache-2.0，证明代码仓库的许可声明，不自动证明新闻原文、作者画像或其他第三方数据的再发布权。本次是重新实现数据契约和 API，未直接复制源页面/素材。隔离依赖环境运行 `uv run --locked python -m pytest tests/triage -q -p no:cacheprovider`：307 项通过；系统 Python 环境首次收集失败是缺少 `langchain_text_splitters` 等依赖，并非测试结论。

## 3. 数据映射与质量闸门

| 源记录 | AI4S 目标 | 映射决定 |
|---|---|---|
| `entity_registry.type=机构/作者` | 新 `impact_entity.kind=institution/author` | **主体**是排名对象；机构可有多个团队，作者可参与多个团队。绝不直接写 `strategic_map_team` 或 `strategic_map_person`。人工证据可写独立 `impact_team_link`；导入未创建任何团队关联。 |
| 源 L1 `高原/高峰` | 新 `impact_direction` L1 | 保留为来源层级，不等于现有六大领域。 |
| 源六个 L2 | 新 `impact_direction` L2 的 `ai4s_domain_id` | 通过名称交叉表映射六大领域（`通用AI`→`通用 AI`）；现有域 ID 只读解析。 |
| 源 20 个 L3 | 新 `impact_direction` L3 | 保留源树与审核状态；**不自动映射**到 AI4S 的 25 个现有子领域，不修改团队/图谱筛选。 |
| 源 `events`/`event_sources` | 新 `impact_event`/`impact_event_source` | 保留源 ID、事件原日期、来源 URL。40 条无主体、21 条无方向的事件仍保留，不强制猜归属。 |
| 源 `score_history` | 新 `impact_score_period` | 保留 281 条原始分、生效分、理由、期次与 A/B/C，版本记 `hyperextract-v5-source`、状态 `uncalibrated`。不写团队总分。 |

只读源库与目标隔离库逐表对账：类目 28（L1 2、L2 6、L3 20）、主体 208（机构 199、作者 9）、方向关联 218、主体别名 31、L3 标签别名 4、事件 460、来源 465、评分期次 281、源树历史快照 22，全部一致；源待审簇成员为 0，若后续源库出现此类成员，导入器会拒绝无映射迁移。源库 10 份 markdown 日报覆盖 9 月 1–10 日，抽查首尾两份，未作为可执行事实导入。9 月 1 日报告的备注载有“2026-09-24 模型调整”，说明历史日报曾被回写；报告文字可按固定输入重现，不代表历史文件不可变。评分期次延续至 9 月 24 日，不能把“有分数”误读为“每天都有日报”。源当前 208 个主体的 `country` 均为 `zn`，国外筛选为空属真实数据现状。最近一期评分覆盖 198 个主体，A 2、B 87、C 109；不能用全期 281 行统计当前等级。

**老师指出的数据问题得到实证**：源库 ID 21“智谱”有 14 条事件、3 期分数；ID 41“Z.ai”有 8 条事件、1 期分数，当前别名表未把两者归并。源库 ID 196“集智俱乐部”有 5 条事件且最近为 B。`aliases.py:20-66` 的期刊/媒体和国名拦截未覆盖“俱乐部”。9 月 10 日报告还列出“成都市公安局交通警察支队”“中国计算机学会”等，需要审核其科研主体资格。迁移把名称命中社群、协会、学会、行政执法单位等记录列为**候选**，默认榜单隔离待核查主体；可显式查看，但不自动合并历史分数、删除事件或断言该机构性质。源与战略地图按机构名精确匹配只得到 20 条“主体↔团队”候选关系，其中同一机构可对应多支团队；9 名源作者与现有 443 名人物无精确姓名命中，**姓名匹配不能作为自动团队关联**。

树外池的 21 条事件都保留 `pending_label`：20 条标“具身智能”、1 条标“可重复火箭”；“具身智能”与既有树节点撞名。源 9 月 1 日和 10 日的日报都报告该簇撞名而未建待审节点，说明单靠增大事件数不会让这个方向自动长出正确节点。AI4S 页面显示树外标签、撞名和原始事件，正式 L2 的新建与回溯留待领域映射审核后执行。当前 5 个审核案例牵涉 6 个已评分主体，默认榜单为 192 个；显式“包含待核查主体”显示全部 198 个。

## 4. 本次分阶段交付及回滚

| 阶段 | 已落地 | 回滚边界 |
|---|---|---|
| 1. 契约与迁移 | `ai4s-tool/ai4s_tool/api/impact_store.py` 为独立 SQLite v4；`scripts/reconcile_impact_triage.py` 默认只读预检，`--apply --target` 只允许**新目标文件**。源库以 `mode=ro` 打开，逐表对账；本机新建 `ai4s-tool/impact_triage.db`，各试验版留 `impact_triage.db.bak-*`。源树快照只作只读历史，不能直接对 AI4S 树执行回滚。 | 停用新路由，保留或移走新隔离库；`strategic_map.db` 和源 `triage.db` 未修改。 |
| 2. 排名/审核/日报 | `impact_triage.py` 暴露榜单、主体证据/期次、类目候选/审核/单项回滚、资格/身份审核、确定性日报；React `ui/src/pages/ImpactTriage/index.tsx` 接入独立页面。审核只改隔离库，类目候选不自动成为战略地图子领域。 | 单项审核可经审计 ID 回滚；列表与页面可独立停用。 |
| 3. 增量入口 | `POST /ingest` 仅接收已核准主体、已核准正式 L2、无待审身份冲突且有 HTTP(S) 来源的人工核验事件；批次幂等、每次最多 50 条、模型调用计数恒为 0，默认由 `AI4S_IMPACT_MANUAL_INGEST_ENABLED=0` 禁用。 | 不触发扫描或自动评分；可关闭入口。生产付费日更未启用。 |

## 5. 待校准阈值和待验证项

- 源 `config.py:89-170` 的 0.4/0.3/0.3 权重、EMA α=0.7、15 分异动、A 级需近 730 天旗舰+成就≥70+趋势≥60、B 级成就≥50 或趋势≥75、连续两期降级、方向折扣 0.6、fetch≥60、聚类余弦 0.75、L2 自动固化 5 个成员且内聚度≥0.8、L3 事件数≥3，均须按 AI4S 多领域标注样本校准。当前 L3 的 3 条只用于**候选显示**。源 `calibrate.py:10,172-275` 也明确单期横截面不能校准 EMA、异动或方向折扣。
- 源 `_flagship_count`（`runner.py:276-289`）把缺日期旗舰按近两年计；AI4S 不能无证据沿用。来源 URL 非空不等于真实一手证据；需要核验发表时间、原始机构和成果归属。
- 本轮新测试 `tests/test_impact_triage.py` 5 项通过；前端 `tsc -b` 通过；隔离浏览器脚本覆盖实际 API 的榜单、待审隔离、证据下钻、类目与日报，拦截一切写请求；战略地图前端 7 项、只读数据审计 6/25/186/62、无自动扫描浏览器回归通过。现有战略地图 Python 指定测试在项目 `.venv` 中运行，除本轮新增测试夹具曾有一处失败并修正外，其余 43 项通过；修正后新增 5 项通过。未做真实付费扫描或跨学科人工质量标注。
- 本机旧 Python 工具服务监听 1601，启动于新路由加入之前。自动执行策略拒绝停止/重启该进程的命令，因此正式 `http://localhost:3000/tool/v1/impact-triage/status` 仍为 404；隔离 1602 API + 3001 Vite 浏览器验收为通过。正式服务热切换及 3000 浏览器复验尚未完成。
- 需要用户定夺：智谱/Z.ai 是否同一法律/运营主体、若同一应采用何种统一身份与重评历史；集智俱乐部等社群/协会/执法单位是否具备“科研主体”入榜资格；“具身智能”撞名的 L2 是否需要新方向及其六大领域映射；每日付费检索/模型预算和失败重试上限。未取得这些结论之前，冲突主体保持隔离，自动评分与定时付费日更保持关闭。
