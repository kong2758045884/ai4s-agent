# AI4S 生产更新记录（2026-09-26）

## 已发布

- 地址：http://81.71.163.184/workspace/strategic-map
- 发布目录：`/www/wwwroot/lab/releases/ai4s-20260926T054442`
- 前端生产构建、Java 21 后端、Python 工具服务、Hyper 图谱扫描适配服务已切换；沿用现有 MySQL 和生产凭证。
- 战略图谱公开目录：116 支已核实团队、6 个领域。
- 两套来源图谱：国内 1,179 节点 / 1,481 边，国际 244 节点 / 315 边。
- 分诊库：208 个主体、460 条事件、281 个评分期次；正式机构榜按已审核资格筛选。

## 数据迁移

- MySQL 33 张表逐表对账，原有数据保留；33,876 条工作记忆消息、211 个访客记录未被替换。仅执行幂等的研判报告提示词修订。
- 战略 SQLite 以本地新版快照为基础，合并线上历史行，共保留 558 条原始团队记录、979 条人员记录。
- 线上 54 个旧类目节点归档，原始行内容存入 `deployment_archive`，公开页面使用新版六大领域。
- 对机构与团队名称都精确唯一匹配的记录，仅按人工修改时间迁移显式编辑字段；保留审计记录。
- 合并后 SQLite `integrity_check` 和外键检查通过；公开目录仍为 116 支，不将历史机构或未核实记录冒充新团队。
- 切换时再次读取已停止写入的线上数据库，包含首次备份后新增的 2 条研判记录。

## 验证

- 前端 TypeScript / Vite 构建通过；Java 全模块生产打包通过（部署时未重跑 Java 单测）。
- 推荐及已核实团队目录回归测试：12 项通过。
- 公网首页、Java 健康接口、融合状态接口、已核实团队目录接口均返回 HTTP 200。
- 公网 1440 和 390 像素浏览器验收通过：图谱实际绘制、团队证据切换、404 错误与重试、机构榜、跨期曲线、类目树、每日报告、无页面横向溢出、无未捕获 JS 异常。
- 浏览器扫描交互使用拦截响应；没有调用付费模型或搜索。
- Python、Java、Hyper 适配器、MySQL 四个 systemd 服务处于 active 状态。

## 备份与回滚

- 备份：`/www/wwwroot/lab/release-backups/ai4s-20260926T054442`，含 MySQL 压缩完整备份、SQLite 一致性备份、服务配置及服务器原配置。
- 原 SQLite 数据库仍在 `/www/wwwroot/lab/reactor-tool`，未直接覆盖。
- 原前端保留在 `/www/wwwroot/lab/ui/dist.before-ai4s-20260926T054442`；新前端也保留旧哈希静态资源，兼容已打开的浏览器标签页。
- 回滚命令：

```sh
python3 /www/wwwroot/lab/releases/ai4s-20260926T054442/scripts/activate_server_release.py ai4s-20260926T054442 rollback
```

回滚切换应用与前端，保留新版数据库；不会用备份强行覆盖上线后产生的数据。若已在新版录入数据，应先对账再决定数据回迁。

## 实际限制

- 自动付费扫描、启动扫描和付费嵌入健康探针保持关闭；适配器只在显式扫描请求后运行。
- 真实付费搜索、模型调用未验收；A/B/C 校准、自动付费日更的状态与既有验收报告一致，不因本次部署视为已完成。
- 服务器剩余磁盘约 2.1 GB；保留了本次必要的备份和回滚版本。
- 原有 ES 列值索引服务不可用时后端降级；战略图谱页面已通过公网验收。
- Git 提交仅包含源码、测试、脚本与报告；数据库、生产凭证、浏览器缓存、依赖缓存和本地部署包留在本地或服务器。

## 追加修复：首页侧栏仍显示旧版（2026-09-26）

- 代码核对：`/?view=strategic-map` 的 Home 内嵌入口与 `/workspace/strategic-map` 都引用 `pages/StrategicMap`，没有分别指向新旧实现。
- 现场核对：两个 URL 返回相同的新版 HTML 入口，但部署前 HTML 未设置 Cache-Control；旧标签页可继续运行旧脚本。用户截图的旧界面与此一致。
- 修复：Nginx 的 `/index.html` 和 SPA 入口返回 `no-cache, no-store, must-revalidate`。哈希 JS/CSS 保持长期缓存。
- 修复：点击侧栏“战略图谱”时检查最新 HTML 入口；发现版本不同则完整导航到新版，携带原目标视图。网络检查失败时仍允许打开当前工作区。
- 新前端位于 `releases/ai4s-20260926T054442/ui/navigation-20260926/dist`，原子切换 symlink；没有重新迁移数据库、重启后端或运行付费扫描。
- 版本检查、侧栏和导航单测 11 项通过，TypeScript / Vite 生产构建通过。新增 `ui/scripts/verify-home-strategic-entry.mjs`，覆盖首页点击侧栏、四个融合视图、真实图谱、用户旧链接、普通刷新及 HTML 缓存头。访客身份和空对话列表使用浏览器拦截数据；图谱及团队接口访问生产真实数据。
- Nginx 配置备份：`/etc/ai4s/nginx-locations.before-navigation-20260926.conf`。完整代码回滚仍使用前述 rollback 命令，已兼容追加的前端目录。
