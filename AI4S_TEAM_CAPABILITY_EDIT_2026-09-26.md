# AI / 科学独立编辑修复（2026-09-26）

## 已完成

- 列表恢复 AI / 科学两个独立等级；右侧上方显示相同字段，编辑时为两个下拉框。取消页面上的“双高判断”文字字段，数据库原有文字仍保留。
- 单独修改一个维度只覆盖该维度。保存返回值即时同步列表，刷新继续保留；证据评分、任务匹配分保持独立。
- 新的模型研判结构支持两项初评、理由和原文引用，验证引用后发布。按字段区分 AI 初判、人工修改、未初评；人工操作记录变更前值和提交值，后续模型研判不能覆盖人工等级。
- 不把历史种子数据的“较高”模板冒充模型初评。读取接口使用轻量历史投影，不迁移或重写数据库。
- 核心方向只显示三行，超出使用省略号；团队负责人旁增加“查看完整详情”，详情保留全文。公开目录也保留人工保存的完整方向，防止刷新替换为短标签。
- 只应用编辑字段的响应，保持团队目录已经发布的简介、来源和人员投影。

## 验证

- 后端：团队研判与公开目录 46 项测试通过，包含单维度人工覆盖、另一维度继续 AI 更新、引文校验、旧模板不冒充初评、评分隔离、GET 无数据库写入、完整方向保留。
- 前端：现有页面与展示逻辑 5 项测试通过；TypeScript 构建检查通过。
- Chrome：1440 / 1366 / 390 像素分别验收保存、列表和详情同步、刷新、来源标记、三行省略及完整详情；1366 下两项编辑框首屏可见，390 下列表仍显示两项等级。
- 写入验收仅对 `runtime/team-capabilities-preview-20260926-123609/strategic_map.db` 副本、1605 服务进行，浏览器拦截并改写所有战略图谱 API 请求。当前预览数据库没有验收文字。
- 本地预览仍为 http://localhost:3000/workspace/strategic-map ，Python 服务使用1604和既有预览库。定时扫描保持关闭，没有执行付费调用。

## 尚未执行

- 现有116支公开团队没有两项带依据的 AI 初评历史，因此这232项维度显示未初评。已接入后续模型研判生成流程，但本轮未对存量团队调用付费模型批量重评，也不声称完成了该数据补齐。
- 模型新增输出结构已用合成回放验证；真实服务的初评质量与等级校准需在后续真实研判中核验。

## 数据与回退

未覆盖原数据库，没有新增数据库列。原自由文本、评分和人工记录保留；本轮已有改动继续留在工作树。回退界面或接口时应仅撤销本次对应修改，不能重置整个已有工作树。测试副本用于验收，不可替换用户正在预览的数据库。
