# AI4S 主体影响力融合与优化计划（2026-09-25）

> 本文件记录实施前的优化路径，部分“待实施/生产未接入”状态已过时。当前同页融合、3000 本地验收及剩余边界以 [最新实施报告](AI4S_STRATEGIC_MAP_INTEGRATION_REPORT_2026-09-25.md) 为准。

## 目标与口径

老师打开页面后，应能迅速回答：哪些**中国内地科研机构**在某方向有可核实成果、为什么上榜、比上一期发生什么变化、原始来源在哪里、今天新增了什么。榜单主体是机构，不是作者、品牌、社群、协会或行政执法单位；战略地图的“团队”是机构下可独立核验的研究团队，两个对象之间只用有证据的关系连接，评分不相互覆盖。

目前“正式榜”是**资格已核验、来源分数待校准的演示榜**。仅四个官网核验样本：中国科学院、清华大学、复旦大学、浙江大学。这里的源 A/B/C、总分与日期保留为历史参考，不能视为 AI4S 重新评分的科学结论。香港院校暂不按“中国内地”自动纳入；企业品牌不等于具体研究机构，只有能独立证明身份与研发职责的企业研究院/实验室才可进入资格审核。

## 代码和数据证据

| 主题 | 源系统与现项目事实 | 融合规则 |
|---|---|---|
| 事件入库 | 源 `hyperextract/triage/ingest.py:243-273,323-382,425-601` 完成标题聚类、主体别名/模型消歧、L2/L3 标记及来源写入；本项目原战略地图 `ai4s_tool/api/strategic_map.py:115-155` 保存团队与证据摘要。 | 事件、原文来源、主体身份单独入隔离 sidecar；团队保持原库，只有审核过的 `impact_team_link` 才跨边界。 |
| 类目 | 源 `tree.py:103-269,545-599,694-758,1406-1466` 有未归类池、待审/正式、L3 生长和树快照回滚。源 L1 是“高原/高峰”，L2 六个方向映射 AI4S 六大领域，L3 不等于现有 25 个子领域。 | 保留源树和事件归属；只有审核过的 L2 映射可进入跨项目导航，L3 不能自动写战略地图。候选数 3 与余弦阈值只是源口径。 |
| 评分 | 源 `scoring.py:97-166` 用模型评三维，稳定层由 `stability.py` 执行 EMA、异动复核与降级滞回；现团队评分是 `strategic_map.py:85` 的 `hybrid-evidence-v3`。 | `impact_score_period` 保存所有来源期次、模型版本、理由和原始来源；不取源 A/B/C 当团队分，不把两个主体旧分数相加。AI4S 新版评分需按同一证据集回放、标版本。 |
| 日报 | 源 `daily.py:114-177,304-452` 从增量结果确定性组装报告，但上游模型标注不确定。 | 日报只消费固定的已入库批次、审核记录和评分期次；要保存输入批次/版本/生成时间，使历史日报可重放。源 9 月 1 日报告曾在 9 月 24 日回写，不能只靠文件名假设不可变。 |
| 前端 | 源看板是脚本生成的静态 HTML；当前项目 React 在线路由为 `ui/src/pages/ImpactTriage/index.tsx`，API 在 `impact_triage.py`。 | 吸收筛选、证据下钻、树审核、日报的信息组织，不复制源静态页面作为生产集成。 |

迁移对账：源 28 类目、208 主体（199 机构、9 作者）、218 主体方向关系、31 别名、460 事件、465 来源、281 评分期次、22 树快照已逐表导入并在新库对账。198 个主体有最新评分，源 A2/B87/C109；这些不是正式可入榜数量。源 `智谱` 14 事件/3 期、`Z.ai` 8 事件/1 期，另有 `Zhipu AI` 2 事件/1 期；[智谱官网关于页](https://www.zhipuai.cn/zh/about)使用“智谱（Z.AI）”，[Z.ai 公司页](https://z.ai/company)载明 ZhipuAI 发展历程，故三条记录归一为一条展示身份，但 24 事件与五个原评分期次仍分别可追溯。`Zhipu AI` 的两条来源事件含传闻基准，不能直接充当评分成果。源 20 条“具身智能”树外事件与已有同名 L3 撞名，现已归入该 L3 的父 L2，未新建节点；1 条“可重复火箭”仍在未归类池。以上只修改隔离库，源库和战略地图库未覆盖。

## 已落地的阶段（本轮）

1. **隔离迁移与准入**：`scripts/reconcile_impact_triage.py` 只读源审计，`--apply` 只接受全新目标库；`impact_store.py` 升到 schema 6。正式榜 API 只返回 `country=zn`、机构类型、已核资格、一手 HTTPS 依据和明确科研机构类型的主体。参考页保留未核验档案且与正式榜分开。四个核验样本的依据来自[中科院官网](https://www.cas.cn/xxgkml/zgkxyyb/jgsz/jbqk/)、[清华官网](https://www.tsinghua.edu.cn/xxgk.htm)、[复旦官网](https://www.fudan.edu.cn/18/list.htm)、[浙大官网](https://www.zju.edu.cn/xxgk/17944/list.htm)。不按名称或来源系统“机构”标签批量放行。
2. **身份与类目纠错**：`scripts/resolve_known_impact_cases.py` 干跑预检、SQLite 备份后写入身份链接和事件映射。`Z.ai` 与 `Zhipu AI` 不再各占一条参考榜行；旧事件和分数留存，详情标“待重评”。身份链接与“具身智能”事件映射均有单项撤销 API，且脚本备份能恢复整个隔离库。
3. **老师演示页**：React 页提供资格榜/来源参考切换、方向/等级/关注筛选、主体/品牌/别名/事件关键词检索、证据原文、跨期分数、L1→L3 树、审核回滚及确定性日报。搜索在 180 ms 防抖后查询本地快照；这是本地 460 事件规模的能力，尚非全网检索。正式榜用一手来源约束，搜索命中显示机构、别名或事件依据。
4. **验收**：源系统分诊测试 307 项通过；新增 AI4S Python 测试 7 项覆盖对账、资格、身份归一、事件映射、回滚、日报；TypeScript 与 ESLint 通过；隔离浏览器验收覆盖 1440×900 桌面和 390×844 手机页面、证据下钻、无横向整页溢出与无写请求。生产 3000 所接的旧 Python 进程尚未载入新路由，因此演示应先用隔离预览 `http://127.0.0.1:3002/workspace/impact-triage`，生产入口仍需部署后复验。

## 下一阶段：按优先级执行

### P0 · 让页面可信且老师看得清（先于扩大来源）

- **信息层级**：首屏显示上榜资格数量、最近更新时间、今日增量、证据缺口；方向卡片明确“当前机构数”和“来源评分待校准”。“参考档案”按名称/匹配度而非旧评分排序，作者与非科研机构只能作证据对象，不进入正式榜。
- **检索**：建立实体名/历史别名/事件标题/摘要/来源标题的 FTS5 索引，按字段权重排序；给出命中片段、日期、原文链接、结果总数和分页。保留零结果、请求失败、数据过期、来源失效的明确状态。目标是本地索引查询 p95 < 250 ms（需在目标机器实测），前端输入到结果 p95 < 500 ms。
- **摘要**：方向摘要固定列出最近 7 天新增、代表成果、反证/争议、来源覆盖、结论置信度；每句话可点回事件和原文。信息不足时写“暂无足够证据”，避免通用套话。桌面 1440×900、笔记本 1366×768 和手机宽度分别验收首屏、表格和详情抽屉。
- **验收**：老师拿 5 个不同方向问题检索，逐条核查来源、归属、摘要、时效；截屏审阅主榜、详情、树、日报；无横向溢出和误导性“已核”标签。

### P1 · 每日机构身份归一（落实截图中的建议）

1. 每日增量先用规范化名称、英文/中文别名、官网域名、论文机构字段生成**候选对**；排除“总院/分院”“学校/实验室”“母公司/产品品牌”这种仅有关联但不同层级的组合。智谱/Z.ai 是品牌身份归一示例，不能照搬成通用字符串规则。
2. 大模型只判断候选关系类型并给出引用证据：`same_legal_entity`、`brand_of`、`subsidiary_of`、`part_of`、`unrelated`、`insufficient_evidence`。输出固定 JSON 和原文 URL；一手来源一致且关系清晰才自动生成待审归一建议。模型的单次结论本身不直接改主体表。
3. 审核确认后写 `impact_identity_link` 与审计记录，页面合并显示事件；所有来源主体 ID、别名、原评分期次不动。异议时撤销链接，自动恢复两条展示记录。每天记录候选数、模型判定数、人工确认/否决数、误合并回滚数。
4. 归一影响评分时，从同一天的证据并集去重重评，历史按事件可见日期回放；新 `score_version` 另写期次，不覆盖原 281 条。高风险同日重复来源、母子机构归属、跨法人品牌须人工复核。

### P2 · A/B/C 科学校准与类目生长

- 在至少 30 个跨领域国内科研机构样本上人工标注成就、学术地位、近 90 天趋势与来源质量；同一机构不同方向单独看，避免大机构凭规模压过专业研究所。评估源三维 0.4/0.3/0.3、A/B/C 门槛、EMA 系数、异动阈值、降级滞回的误报/漏报；保留源期次作对照，不直接移植阈值。
- 将“候选标签≥3 事件”改成可配置且按来源去重后的阈值；结合跨来源独立性、持续天数、与已有 L3 的语义距离。优先归一同义 L3，再决定新 L3；新 L2 必须指定六大领域映射并经过审核。源 `具身智能` 撞名案例加入回归样本。
- 每次评分/类目写入记录规则版本、审核人、批次 ID、旧值、新值；支持单项回滚和批次备份。资格变更后重算展示范围与日报，不抹掉历史来源。

### P3 · 广而细的增量检索与确定性日报

- 来源分层：机构官网、论文/预印本、公开项目/基金、会议、专利、权威新闻；对每条记录保存标题、URL、发布日期、抓取日期、主体候选、方向、证据片段、许可/可访问状态。热门方向可扩展搜索词，但只用可追溯原文进入评分。
- 以来源 URL/外部 ID + 内容哈希保证重试幂等；URL 相同内容变化作为新版本。每日对近 7 天窗口补抓延迟发布，日报分清“事件发生日、首次发现日、本次入库日”。同一事件多来源聚合，不重复计成就。
- 日报固定顺序“数据流入 → 类目审核 → 身份归一 → 排名变化 → 未完成/失败”；保存查询所依赖的批次和评分版本。生成后设版本号，后补证据生成修订版，不悄悄改旧日报。
- 来源数和费用不限额是**当前预算策略**，不是无限重试。单请求 429/5xx 指数退避加随机抖动，最多 3 次；确定性 4xx 不重试；同一任务失败入延迟队列，次日继续。按提供方限流控制并发（初始 4，测量后调整），记录 token/检索调用/费用，但不设每日人民币上限。异常费用突增或提供方故障触发熔断和可见告警，防止循环烧费。启用真实定时任务前完成沙盒回放、来源质量抽样和页面验收；本轮未运行付费扫描。

### P4 · 生产接入与长期验收

- 在已备份且验收的环境中更新 Python 路由，核对 Vite `/tool` 代理、鉴权与只读/审核权限；生产 3000/1601 路由、浏览器主流程和旧战略地图筛选回归全部通过后再切入口。当前旧进程未更新，隔离预览通过不代表生产已上线。
- 对 4 个已核样本之外逐批审核：每批核验 20 个，记录来源 URL 与不入榜原因；社群、协会、执法单位、作者和品牌默认只能作事件相关对象。香港/澳门机构、海外分支与企业研发机构的详细边界若老师另有口径，再更新资格规则及历史重评。
- 目标验收样本：同机构不同品牌、同名不同机构、母子机构、跨期改名、作者/团队/机构混淆、具身智能撞名、迟到证据、来源失效、模型拒答、429 限流、日报修订与整批回滚。每周抽查误合并率、入榜误收率、摘要引文准确率与页面检索时延。

## 回滚和待验证边界

- 本轮隔离库备份：`ai4s-tool/impact_triage.db.before-known-cases-20260925-204826.bak`、`impact_triage.db.before-verified-seed-20260925-205143.bak`，后续 schema/audit 变更另有备份；均未覆盖源 `triage.db` 或 `strategic_map.db`。事件归一、身份链接可单项撤销，资格样本可在审核接口重置为 `unreviewed`。
- 已验证的是隔离 API 与浏览器、本地历史快照；未验证的是全网召回率、每日模型去重准确率、AI4S 新评分科学性、生产 3000 服务和付费日更。P1–P4 是工作计划，不是现成功能。
- 现阶段无需用户再次决定智谱/Z.ai、社群入榜或具身智能同名处理；可调整的产品边界仅剩香港/澳门与**独立企业研究院**是否纳入“国内科研机构”，当前按上述保守口径执行。
